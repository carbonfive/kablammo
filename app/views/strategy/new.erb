<h1>Enter Your Robot</h1>
<% if strategy.errors.any? %>
  <div class="alert alert-danger">
    <p>We were unable to save the strategy.</p>
    <ul>
      <% strategy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
<p>
  For instructions on how to create a robot, read the <a href="<%= url('/docs/') %>">docs</a>. For contest info, read the <a href="<%= url('/rules/') %>">rules</a>.
</p>
<form method="post" action="<%= url('/strategies') %>">
  <div class="form-group">
    <label for="github_url">Robot Repo</label>
    <input name="github_url" type="text" id="github_url" class="form-control" placeholder="Your Kablammo Strategy Github SSH clone URL">
  </div>
  <div class="form-group">
    <button type="submit" class="btn btn-primary">Add Robot</button>
  </div>
</form>