<h1>Leaderboard</h1>

<table class="table leaderboard">
  <tbody>
    <% strategies.each_with_index do |strategy, i| %>
      <tr>
        <td>
          <%= i + 1 %>.
        </td>
        <td>
          <a href="/strategies/<%= strategy.id %>">
            <%= strategy.username %>
          </a>
        </td>
        <td>
          <%= scoreboard[strategy.username].nil? ? 'N/A' : scoreboard[strategy.username] %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<p>
  <a href="<%= url('/strategies/new') %>" class="btn btn-primary">Enter Your Robot</a>
</p>